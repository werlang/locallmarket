{
  "name": "api",
  "version": "0.1.0",
  "description": "",
  "type": "module",
  "scripts": {
    "start": "node --inspect=0.0.0.0 --watch app.js",
    "test": "node --test --test-reporter=spec 'test/**/*.mjs'",
    "test:unit": "node --test --test-reporter=spec 'test/unit/**/*.mjs'",
    "test:integration": "node --test --test-reporter=spec 'test/integration/**/*.mjs'"
  },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.3"
  }
}
